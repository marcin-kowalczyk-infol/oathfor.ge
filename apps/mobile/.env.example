# Public development URL only. iOS simulator can use host loopback;
# Android emulator normally uses http://10.0.2.2:18082. Native verification pending.
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:18082
# Development diagnostic fixture, not the product's language-selection policy.
EXPO_PUBLIC_DIAGNOSTIC_LOCALE=pl
