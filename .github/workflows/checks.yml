name: Checks

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - name: Repository structure
        run: |
          python3 .agents/commands/check_repository.py
          git diff --check
      - name: API, PostgreSQL and Redis integration
        run: python3 .agents/commands/check_api.py
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: apps/mobile/package-lock.json
      - name: Mobile tests, types and bundles
        working-directory: apps/mobile
        env:
          EXPO_PUBLIC_API_BASE_URL: http://127.0.0.1:18082
          EXPO_PUBLIC_DIAGNOSTIC_LOCALE: pl
        run: |
          npm ci
          npm test -- --runInBand
          npm run typecheck
          npx expo install --check
          npx expo export --platform ios --platform android
